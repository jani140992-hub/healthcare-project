# CarePulse Enterprise Healthcare System Dependencies
# Zero mandatory external runtime dependencies required for standalone operation.
# The following packages can be installed for high-performance production asynchronous execution:

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
sqlalchemy>=2.0.28
pytest>=8.0.0
pytest-asyncio>=0.23.0
cryptography>=42.0.0
python-multipart>=0.0.9
requests>=2.31.0
